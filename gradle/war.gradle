apply plugin: "war"

bootWar {
    mainClassName = "com.sysco.obte.DpfServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
